[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "icontext"
version = "0.1.0"
description = "Give AI the context it needs from your codebase – without dumping it."
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
dependencies = []
